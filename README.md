# sum_of_two_number_django_with_dbsqllite
A simple Django web application that performs addition of two numbers and stores each operation in a SQLite database. Users can add new operations, view all past results in a table, edit previous entries, and delete them — a basic CRUD application built with Django ORM.
